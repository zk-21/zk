# 本次处理记录

## 主要产物

- `D:\work\张可\点点\test\english-learning.html`
- `D:\work\张可\点点\test\三年级下册语文互动学习.html`
- `D:\work\张可\点点\test\textbook-pages\`
- `D:\work\张可\点点\test\skills\pdf-textbook-workflow\SKILL.md`

## 语文教材处理流程

1. 先核对 HTML 页面里的单元数据和渲染逻辑。
2. 检查 PDF 是否能直接抽文字。
3. 如果是扫描版 PDF，就把教材页导出成图片。
4. 先看目录页，确认单元、课文顺序和页码。
5. 对照 HTML 页面，找出缺课、错位、节选、改写、错误作者等问题。
6. 如果追求准确，就在页面里展示教材原页图片，而不是手打长文。
7. 在每课后补"课文解析、拓展积累、提优挑战、示例/参考答案"等互动内容。
8. 最后检查页面脚本是否正常。

## 下次复用方式

可以直接说：

`用 pdf-textbook-workflow 这个 skill，按照这次的方法处理我给你的教材 PDF。`

或者：

`根据这个教材 PDF，对照当前 HTML 学习页，改成教材版，并补充拓展、提优和可点击参考答案。`

## 2026-06-08 五项优化

### 1. 修复 CSS Bug
- 修复所有 HTML 文件中无效的 `rgba(255,255,255,255,0.25)` 五参数写法

### 2. 清理冗余文件
- 删除 13 个冗余文件（副本、测试音频、测试脚本等）

### 3. 音频压缩
- 715 个 WAV 文件批量压缩为 MP3（64kbps, 22050Hz, libmp3lame）
- 58.06 MB → 10.98 MB，节省 47.09 MB（81%）

### 4. 代码提取（共享 CSS/JS）
- 从 6 个 HTML 文件中提取共享代码：
  - `shared-learning.css`：约 320 行共享样式
  - `shared-learning.js`：约 560 行共享逻辑（语音系统、交互、测验、渲染）
- 每个 HTML 文件从 ~937 行精简到 ~344 行
- 各页面通过 CSS 变量（`--theme-color`, `--theme-gradient`）保持独立主题色

### 5. 统一数据格式
- 三个数据文件统一使用 `u()` helper 函数格式：
  - `grade3a-data.js`：已是 `u()` 格式（无需修改）
  - `grade4a-data.js`：从 plain object 格式转换为 `u()` 格式（578→551 行）
  - `grade4b-data.js`：将 `unit()` 重命名为 `u()`
- `COURSE_GRAMMAR_GUIDE` 部分保持原始格式不变

## 2026-06-08 增加发音规则、语法口诀、背诵技巧

### 三~四年级页面（grade4-learning.js 渲染系统）
- 新增 `window.COURSE_TIPS` 数据结构，追加到三个数据文件末尾：
  - `grade3a-data.js`：8 个单元的发音/语法/记忆/文化提示
  - `grade4a-data.js`：8 个单元的发音/语法/记忆/文化提示
  - `grade4b-data.js`：8 个单元的发音/语法/记忆/文化提示
- `grade4-learning.js` 新增 `renderLearningTips()` 函数，渲染"学习加油站"面板
- `grade4-learning.css` 新增 `.tips-grid`, `.tip-card`, `.tip-card-header`, `.tip-card-body` 等样式
- 每个单元包含 4 类提示卡片：🔤 发音规则、📝 语法口诀、💡 背诵技巧、🌍 文化小贴士

### 一~二年级 & 五年级页面（shared-learning.js 渲染系统）
- 增强 6 个 english-*.html 页面的 4 个提示函数：
  - `getPhonicsTip`：从 1 句话扩展为含 `<span class="tip-rule">` 和 `<span class="tip-example">` 的详细发音规则
  - `getGrammarRhyme`：从简单口诀扩展为结构化语法要点
  - `getStudyTip`：从简短建议扩展为具体可操作的背诵方法
  - `getCultureTip`：保持不变（内容已较好）
- 涉及文件：`english-1a.html`, `english-1b.html`, `english-2a.html`, `english-2b.html`, `english-5a.html`, `english-5b.html`

## 2026-06-08 第二轮优化

### 1. 修复音频格式引用
- `grade4-learning.js` 第 67 行：`.wav` → `.mp3`
- `english-learning.html` 第 1903 行：`.wav` → `.mp3`
- 代码现在引用已压缩的 MP3 文件

### 2. 清理临时脚本
- 删除 15 个一次性 .ps1 脚本（batch-generate-audio-*, check-audio, compress-audio, count-*, generate-*, list-voices）
- 删除 1 个 .py 脚本（generate-wavenet-audio.py）

### 3. 删除孤儿文件
- `english-learning-fixed.html`（117KB，无引用）
- `windows-tts-test.wav`（337KB，测试音频）
- `audio-generation-log.txt`（编码损坏的日志）
- `git`（空文件）

### 4. 语文页面优化
- **CDN 本地化**：Tailwind CSS 和 Google Fonts 改用国内 CDN 镜像（bytecdntp / fonts.loli.net），添加 onerror fallback
- **移除 Font Awesome**：仅用 2 个图标（🔊🔇），改用 emoji 替代，减少一个外部依赖
- **添加年级导航栏**：与其他英语页面一致的 course-switcher 导航，方便在语文/英语/各年级间切换
- **系统字体回退**：添加 PingFang SC / Microsoft YaHei 等系统中文字体，确保离线时正常显示

### 待处理（用户未批准删除）
- 3 个 backup 目录（已被 .gitignore 排除，不影响使用）
- `audio/en/` 下的 .wav 文件（代码已改为引用 .mp3，wav 可手动删除节省 ~47MB）
- `audio/en/` 下 163 对空 clip 文件（无实际音频内容）
- `thumbs/` 和 `pdf_pages/` 目录（未被页面引用的图片）

## 说明

- 如果只是想保留"结果"，保存这些文件就够了。
- 如果想保留"过程"，这份记录可以继续追加。
